import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  input: {
    height: 40,
    padding: 8,
    borderWidth: 1,
    borderRadius: 16,
    marginHorizontal: 16,
  },
});

export { styles };
