import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  divider: {
    width: "100%",
    borderWidth: 1,
    borderColor: "#ececec",
  },
  scrollView: {
    paddingHorizontal: 16,
    paddingVertical: 8,
  },
});

export { styles };
